{
  "buildCommand": "cd client && npm run build && cd ../server && npx prisma generate && npx prisma db push",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
